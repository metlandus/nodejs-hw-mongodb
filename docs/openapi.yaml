openapi: 3.1.0
info:
    version: 1.0.0
    title: Contacts App
    license:
        name: Apache 2.0
        url: http://www.apache.org/licenses/LICENSE-2.0.html
    description: Documentation of Contacts app API
tags:
    - name: Contacts
      description: Operations about users
    - name: bearerAuth
      description: Auth Operations
servers:
    - url: http://127.0.0.1:5173
paths:
    /contacts:
        get:
            $ref: ../swagger/paths/contacts/get.yaml
        post:
            $ref: ../swagger/paths/contacts/post.yaml
    /contacts/{id}:
        get:
            $ref: ../swagger/paths/contacts/{id}/get.yaml
components:
    securitySchemes:
        bearerAuth:
            type: http
            scheme: bearer
